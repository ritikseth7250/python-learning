# Python Learning

A collection of my Python practice programs and learning notes.

## Learning Progress — September 24, 2026

Today I practiced the following Python concepts:

### 1. Functions

- Defining and calling functions
- Passing arguments to functions
- Printing values from inside a function

### 2. Dictionaries

- Creating dictionaries with key-value pairs
- Adding and updating dictionary values
- Looking up values using a key
- Removing values with `pop()`
- Iterating through dictionary items with `.items()`

### 3. User Input

- Reading text with `input()`
- Converting input into numbers with `int()` and `float()`
- Using user input to search dictionaries and update data

### 4. Conditional Statements

- Using `if`, `elif`, and `else`
- Comparing values with `==`
- Checking whether a guessed number matches a random number

### 5. Loops

- Using `while` loops for repeated input
- Stopping a loop with `break`
- Using `for` loops to process lists and dictionaries

### 6. Lists

- Creating empty lists
- Adding items with `.append()`
- Storing foods and prices separately
- Iterating through list values

### 7. Random Numbers

- Importing the `random` module
- Generating random numbers with `random.randint()`
- Building a simple number guessing game

### 8. Basic Programs Practiced

- Student marks lookup
- Student marks entry and dictionary updates
- Random number guessing game
- Shopping cart with food items and prices
- Menu-based food selection

## Practice Files

| File | Concepts Practiced |
| --- | --- |
| `PYTHON/function/learn.py` | Functions and arguments |
| `PYTHON/Random/dect.py` | Dictionaries, input, and menu selection |
| `PYTHON/Random/luckydraw.py` | Dictionaries, input, and updating values |
| `PYTHON/Random/replicate.py` | Random numbers, input, and conditionals |
| `PYTHON/Random/Shopping cart.py` | Lists, loops, input, and calculations |

## Next Steps

- Practice writing functions with correct parameters and return values
- Learn more about error handling with `try` and `except`
- Improve the guessing game with multiple attempts and hints
- Combine functions, lists, and dictionaries into larger programs
- Keep practicing clean formatting and meaningful variable names
